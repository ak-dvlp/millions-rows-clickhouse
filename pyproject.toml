[project]
name = "millions-rows-clickhouse"
version = "0.1.0"
description = "E-commerce data pipeline"
requires-python = ">=3.10, <3.15"  # Четко ограничиваем верхний порог

dependencies = [
    "clickhouse-connect>=1.7.2,<2.0.0"  # Никаких маркеров, пакет обязателен для проекта
]

[tool.poetry]
package-mode = false
